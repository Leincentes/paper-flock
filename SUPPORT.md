# Paper Flock Support

Configure `supportEmail` or `supportUrl` in `app-config.json` before a broad public release. Beta users can generate a local JSON feedback file through the in-game **Send feedback** action.
