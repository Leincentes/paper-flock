# Paper Flock v1.4.4 — Full Fix Source Report

This source package consolidates every fix identified during GitHub qualification.

## Included fixes

1. Public npm registry lockfile with the valid `speedline-core@1.4.3` package.
2. Android WebView debugging detection without relying on generated `BuildConfig`.
3. Android API 36 predictive-back migration using `OnBackPressedDispatcher`.
4. Activity Result API migration for backup import and export.
5. AndroidX edge-to-edge and window-insets migration.
6. Removal of deprecated WebSQL and legacy system-bar calls.
7. Browser startup and modal-focus race-condition fixes.
8. Android API-level theme qualification:
   - `android:windowLightNavigationBar` removed from unqualified API 24 resources;
   - added to `values-v27/themes.xml`;
   - repeated in `values-v31/themes.xml` with the splash-screen resources.

## Local validation

- 209/209 automated tests passed.
- JavaScript syntax checks passed.
- Package audit passed.
- Accessibility/security hardening audit passed.
- Production verification and deterministic release build passed.
- Play Store asset validation passed.
- XML theme resources parsed successfully.
- `speedline-core-1.4.4` is absent from the lockfile.
- `speedline-core-1.4.3` is present.

## GitHub confirmation still required

The artifact environment does not provide an Android SDK, so GitHub Actions must run:

```bash
gradle -p android clean lintDebug bundleDebug --warning-mode all
```

A signed release AAB also requires the repository signing secrets.
